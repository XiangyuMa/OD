package interview;

import java.math.BigDecimal;

public class DailyProduct extends Product{
    public DailyProduct(int mount, BigDecimal price, String desc) {
        super(mount, price, desc);
    }
}
